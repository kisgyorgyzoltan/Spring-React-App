import { createTheme } from '@mui/material';

export const blueTheme = createTheme({
  palette: {
    primary: {
      main: '#2196f3',
    },
    secondary: {
      main: 'rgb(255, 255, 255)',
    },
    background: {
      default: '#fff',
    },
  },
});
